requests>=2.28
numpy>=1.24

# Claude Vision engine (optional — only needed with --engine claude)
anthropic>=0.40

# Local YOLO engine (optional — only needed with --engine yolo)
ultralytics>=8.0
Pillow>=9.0

# Live preview window (optional — only needed with --show)
opencv-python>=4.7
